To create the client keystore::
keytool -import -keystore clientkeystore -file /path/to/cert.der -alias yourcertalias.com

This is an example usage of the BitcoinJ library that acts as a bitcoin client.
